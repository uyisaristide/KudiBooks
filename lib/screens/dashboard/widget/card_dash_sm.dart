import 'package:flutter/material.dart';

class CardDashSmall extends StatelessWidget {
  const CardDashSmall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
